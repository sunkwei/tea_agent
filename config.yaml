# @2026-05-06 gen by claude, 清理真实API密钥，替换为占位符
main_model:
  api_key: YOUR_API_KEY_HERE
  api_url: http://localhost:11434/v1
  model_name: qwen3.6
  options:
    max_tokens: 1024
    num_gpu: 15
    temperature: 0.7

cheap_model:
  api_key: YOUR_API_KEY_HERE
  api_url: http://localhost:11434/v1
  model_name: qwen3.6

# 2026-05-06 gen by claude, 文本向量模型配置（用于主题搜索）
embedding_model:
  api_url: ""          # Embedding API 地址，如 http://localhost:11434/v1
  model_name: ""       # 嵌入模型，如 text-embedding-3-small / bge-m3
  api_key: ""          # 为空则复用 main_model.api_key
  dimension: 0         # 向量维度，0=自动检测

keep_turns: 5
