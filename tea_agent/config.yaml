# @2026-05-06 gen by claude, 清理真实API密钥，替换为占位符
main_model:
  api_key: YOUR_API_KEY_HERE
  api_url: http://localhost:11434/v1
  model_name: qwen3.6
  options:
    max_tokens: 1024
    num_gpu: 15
    temperature: 0.7
    supports_vision: false

cheap_model:
  api_key: YOUR_API_KEY_HERE
  api_url: http://localhost:11434/v1
  model_name: zfujicute/OmniCoder-Qwen3.5-9B-Claude-4.6-Opus-Uncensored-v2-GGUF:latest

# 视觉模型（可选）— 会话输入含图片时自动切换到此模型，无图片时使用主模型
# 示例：小米 MiMo 视觉模型
# vision_model:
#   api_key: YOUR_API_KEY
#   api_url: https://token-plan-cn.xiaomimimo.com/v1
#   model_name: mimo-v2.5
#   max_tokens: 128000
#   options:
#     supports_vision: true
#     supports_reasoning: true

keep_turns: 5

# 思考模式强度配置（可运行时修改）
enable_thinking: true
thinking_strength: 0.7      # 0.0-1.0，思考深度强度
reasoning_effort: auto      # auto/low/medium/high，映射到 OpenAI reasoning_effort
